Create a Python class named "person" that represents a person with basic attributes and a method to introduce
themselves.

Requeriments:
- The class should have the following attributes:
  -named (string): the person's name.
  -age (integer): the person's age.
-The class should have the following method:
  -greet(): This method should print the message:
  "Hello, my name is [name]"
   Replace [name] with the actual value of the name attribute.